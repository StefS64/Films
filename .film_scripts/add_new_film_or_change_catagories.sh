#!/bin/bash

# TODO:
# add flags to clear all not selected categories except all_films and Ranking

# otherwise create links only in the added categories

# also add the ability to create new catagories
# flags:	-n(new instance, overwrites all_instances already created)
#			-u(update doesn't overwrite already given catagories) 
#			-d delete a film
#			ATTENTION the ranking category can only have one instance.
